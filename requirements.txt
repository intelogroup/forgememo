# Forgemem Core
anthropic>=0.28.0
fastmcp>=0.6.0

# Phase 1: HTTP API
flask>=2.3.0
requests>=2.31.0
requests-unixsocket>=0.3.0

# Optional: for production deployment
# gunicorn>=21.0.0
# python-dotenv (already used in existing code)
